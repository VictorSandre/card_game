import { FlippableCardDirective } from './flippable-card.directive';

describe('FlippableCardDirective', () => {
  it('should create an instance', () => {
    const directive = new FlippableCardDirective();
    expect(directive).toBeTruthy();
  });
});
